services:
  tarot-bot:
    build: .
    container_name: tarot-bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./tarot.db:/app/tarot.db
    healthcheck:
      test: ["CMD", "python", "healthcheck.py"]
      interval: 30s
      timeout: 10s
      retries: 3
